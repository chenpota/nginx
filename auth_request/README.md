# Introduction

A sample to use auth_request to implement custom authentication.

# Usage
```
$ make -C docker build
$ make setup
$ make run

# Call http://127.0.0.1/api in browser and input username password.

# Call http://127.0.0.1/logout in browser or close browser to clear cookie.
```